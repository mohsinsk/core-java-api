# Application Mode: 'docker' or 'local'
# - 'docker': Connect to MySQL via 'mysql' service name (for docker-compose)
# - 'local': Connect to MySQL via 127.0.0.1 (for local development)
MODE=local

# Database Configuration URLs
DB_URL_DOCKER=jdbc:mysql://mysql:3306/core_java_api
DB_URL_LOCAL=jdbc:mysql://127.0.0.1:3306/core_java_api

DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=core_java_api

# Authentication
AUTH_TOKEN=Bearer your-secret-token-here

# Environment (optional: development, staging, production)
# ENVIRONMENT=development

